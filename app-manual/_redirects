# Worker app routes — serve worker.html so iOS Safari reads the correct
# manifest at HTML-parse time (before JavaScript runs).
/w/*     /worker.html  200
/worker  /worker.html  200
/worker/* /worker.html  200

# Admin SPA fallback — all other routes serve index.html.
/*       /index.html   200
